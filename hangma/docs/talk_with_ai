# 与 AI 对话记录

## 2026-05-21 — v2.0 架构重构：UI/引擎分离

### 背景
用户要将杭州麻将的实现逻辑去掉，保留 UI，留下接口方便重新实现。用户提供了新 PRD 在 `docs/PRD.md`（从 `E:\CLAUDE\hangzhou_majiang\docs\PRD.md` 复制）。

### 决策
- **模块方案**：ES Modules（import/export），index.html 用 `<script type="module">`
- **架构**：`engine.js`（引擎接口+stub） + `app.js`（纯 UI+流程编排）
- **旧逻辑处理**：直接删除，不留参考。用户将按新 PRD 从头实现。
- **Stub 模式**：engine.js 当前为桩实现，所有规则返回 false/空，UI 可正常渲染空桌。

### 变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/engine.js` | **新建** | 引擎接口定义 + Stub 桩。31 处 `[TODO: IMPLEMENT]`，含完整 JSDoc |
| `src/app.js` | **重写** | UI 层，~950 行。import engine 的所有规则/AI 函数 |
| `src/index.html` | 修改 | `<script type="module" src="./app.js">` |
| `package.json` | 修改 | 加 `"type": "module"` |
| `AGENTS.md` | 更新 | 新架构说明、本地运行方式 |
| `docs/PRD.md` | 新增 | 从 CLAUDE 目录复制 |
| `docs/talk_with_ai` | 更新 | 本记录 |

### engine.js 接口速览

```
导出的常量:
  SUIT_CONFIG, HONOR_LABELS, CHINESE_NUMERALS, SEATS, INITIAL_SCORE

Tile 工具 (纯函数，已实现):
  createTile, tileKey, tileLabel, tileGlyph, tileSortValue, sortTiles,
  cloneTile, tileAssetName, tileAssetPath, isTileWild, shuffle

状态创建 (需实现):
  createWall() → Tile[]     — 136张洗牌
  createPlayers() → Player[] — 已实现
  createNewGame() → GameState — 发牌+定财神

规则判定 (需实现):
  isWinningHand(tiles, wildKey) → boolean
  getWaitTiles(tiles, wildKey) → Tile[]
  getChiOptions(hand, discardTile, wildKey) → tileId[][]
  getConcealedGangChoices(hand, wildKey) → Tile[][]

提示 (需实现):
  getDiscardHints(hand, wildKey) → {discard, waits}[]

AI 决策 (需实现):
  chooseDiscard(hand, playerId, gameState) → Tile
  shouldPeng(playerId, tile, gameState) → boolean
  shouldMeldGang(playerId, tile, gameState) → boolean
  chooseChi(playerId, tile, choices, gameState) → tileId[]|null
  chooseConcealedGang(playerId, choices, gameState) → Tile[]|null
  shouldPiaoCai(playerId, gameState) → boolean
  resolveClaim(claimList, tile, discarderId, gameState) → ClaimItem|null

计分 (需实现):
  calculateWinScore({hand, wildKey, kind, winningTile, piaoCai})
    → { flags: string[], total: number, isSelfDraw: boolean }
```

### 关键 PRD 差异（与旧代码不同）

| 规则 | 旧代码 | PRD |
|------|--------|-----|
| 胡牌 | 自摸 | 只能自摸，无放炮 |
| 计分 | 统一 250 分底，赢家得分 = 底分变动 | 庄家：8×倍率/家；闲家：庄付8×、另两闲付1× |
| 牌型 | 七对、平胡、爆头、杠开 | 16 种牌型，叠加乘法，无封顶 |
| 杠补 | 牌墙末尾 | 倒数第二张，倒数第一张翻开公示 |
| 四风连打 | 无 | 有，触发后庄家掷骰子付分，本局重开 |
| 飘财 | 2 倍 | 一飘4倍、二飘8倍、三飘16倍，参与牌型叠加 |
| 财神 | 固定白板 | 固定白板（一致） |
| 飘财期间 | 不可吃碰杠 | 不可吃碰明杠，但可暗杠、可自摸胡 |

### 待办（下一会话）
1. 实现 `engine.js` 中所有 `[TODO: IMPLEMENT]` 标记的函数
2. 按 PRD 规则实现 16 种牌型判定
3. 更新 `smoke-test.js` 测试用例
4. 实现四风连打检测
5. 杠后从倒数第二张补牌 + 倒数第一张公示

### 本地运行
```powershell
cd E:\repos\hangma\src
npx serve .          # 或 python -m http.server 8899
# 浏览器打开 http://localhost:8899 (或 :3000)
```

### 不做的特性（初版）
- 网络对战
- 道具/皮肤
- 教练模式（PRD 第8-10节，后续版本）

---

## 2026-05-18 — v1.1 飘财 + AI 策略重训

### 决策
- **飘财规则**：新增听牌后亮财神机制，胡牌双倍计分。飘财后自动摸打，不可吃碰杠。纯自摸规则（无点炮，用户确认意如此）。
- **AI 重训**：
  - 重写 evaluateShape：区分两面搭子(+20)、边张搭子(+10)、坎张搭子(+8)、完整顺子(+28)
  - 新增 evaluateWaitQuality：听牌质量 = 剩余牌数 × 多样性加成
  - 重写 estimateDiscardDanger：考虑所有对手弃牌、对手副露数、终盘曲线
  - evaluateImprovingDraws 新增 shapeGain 追踪
  - 新增 shouldAiPiaoCai：AI 判断是否飘财（听牌数≥5 且质量够好）
- **测试**：smoke-test 从 7 项扩展到 8 项，补全 VM 沙箱 stub（localStorage、AudioContext、setTimeout no-op）

### 未做
- 点炮胡（用户明确不需要）
- 抢杠（暂缓）
- 一炮多响（暂缓）
- 飘财专用音效（用碰牌音效占位）

## 2026-05-18 — v1.1.1 财神（癞子）胡牌 bug 修复

### 问题
用户反馈：白板做将牌时，明明听牌等任意一张就能胡，但进张后胡不了。

### 根因
`getCanonicalCounts()` 将第一张财神"扣留"为普通白板（`reserved = min(rawWildCount, 1)`），导致 `wildCount = 0`。
手牌 4 组面子 + 单张白板听牌时，白板在 counts 中 count=1，无法满足：
- `count >= 2`（不能做天然对子）
- `count >= 1 && wildCount >= 1`（wildCount=0 无法用癞子补对子）

结果：摸任何牌都无法胡。

### 修复
- `getCanonicalCounts`：移除 reserved 机制，所有财神牌全部计入 wildCount，不放入 counts
- `countWildcards`：移除 `-1`，直接返回财神数量（1 张财神即算"财神入手"）
- `isBaotou`：恢复原始判定结构（移除胡牌+1癞子→12张成4面子=爆头），配合新的 wildCount 正确运作。爆头定义：手牌有1张财神且已听牌，摸任意牌可胡。
