# Vibe Coding Rules — DSP28335 模板工程

> 本文件定义 AI Agent 在本工程中的行为约束。AI 在每次对话开始时必须读取并遵守。

---

## 规则 1: 流程遵守
- 新会话启动时，必须按顺序阅读四件套文档：`README.md` → `PRD.md` → `IMPLEMENTATION_PLAN.md` → `SESSION_HANDOFF.md`
- 学习文档（`LEARNING_NOTES.md`、`docs/`）在涉及新知识点时阅读并按规则更新
- 必须严格按照 README.md 定义的四阶段流程工作
- PRD 未确认 → 不得编码
- 实施计划未确认 → 不得编码
- 实施计划中某步为 `[ ]` 且其依赖未完成 → 不得开始该步
- 实施计划采用**函数级微步骤**：每步只实现 1 个函数（或 1 个紧密函数对），可独立编译、可独立验证

## 规则 2: 文件修改边界
- 每个实施步骤只允许修改该步骤明确列出的文件
- 禁止修改 TI 外设库文件 (`SRC/DSP2833x_*.c/.asm`, `INCLUDE/DSP2833x_*.h`)
- 禁止修改链接器脚本 (`CMD/*.cmd`, `28335_RAM_lnk.cmd`)
- 禁止修改工程构建配置 (`.ccsproject`, `.cproject`, `.settings/`)

## 规则 3: 进度跟踪、学习讲解与会话交接
- 每完成一个实施步骤后, 必须立即更新 `IMPLEMENTATION_PLAN.md`:
  - 将该步的 `[ ]` 改为 `[x]`，在行末添加完成日期
- 每完成一个步骤，**必须按顺序完成四件事**（缺一不可）：
  1. 填写该步骤的"开发讲解"（用到的 TI 库函数/为什么这样做/硬件原理一句话）
  2. 把新知识点追加到 `LEARNING_NOTES.md`（按主题，用大白话）
  3. 用大白话向用户讲解本步，并询问"这一步看懂了吗"
  4. **用户未确认看懂 → 必须换更简单的说法重讲，不得继续下一步**
- 完成后必须向用户汇报并明确询问是否继续下一步
- 不得在用户未确认的情况下连续执行多个步骤
- **每次会话结束前**，必须更新 `SESSION_HANDOFF.md`：追加一条交接记录（进度快照/已完成功能/遗留问题/未提交改动/下一步指引），供下一个 AI 窗口接力

## 规则 4: 代码质量
- 每个 .c 文件必须有文件头注释 (`@file`, `@brief`)
- 每个公开函数必须有函数头注释 (用途, 参数, 返回值)
- **封装函数优先（双层策略）**：
  - 有 TI 库函数/封装函数时必须使用封装（如 `InitSysCtrl()`、`InitEPwm1Gpio()`、`DELAY_US()`、`ConfigCpuTimer()`）
  - **应用层**（MAIN.c 主循环/算法/ISR 逻辑）禁止裸寄存器操作
  - **驱动层**（APP_CONFIG.c）允许寄存器操作，但每行必须中文注释解释"这个寄存器在做什么"
  - 例外：TI 无封装的必要操作（如 `PieCtrlRegs.PIEACK.all`、`EALLOW/EDIS` 向量注册）允许，但必须注释
- **注释必须使用简体中文，禁止英文描述性注释**（例外：代码标识符/寄存器名/宏名/Doxygen 标签/C 关键字/技术缩写）
- 每次提交前必须运行注释检查：`python tools/check_comments.py`，必须输出 `[通过]`
- ISR 末尾必须: 清除外设中断标志 → `PieCtrlRegs.PIEACK.all = PIEACK_GROUPx`
- CPU Timer ISR 必须: `.TIF=1; .TRB=1;` → `.PIEACK.all = PIEACK_GROUP1`
- 禁止在 ISR 中调用阻塞函数或使用 `for(;;)` 等忙等循环
- 跨 ISR/主循环的共享变量必须用 `volatile` 修饰

## 规则 5: 安全编码
- 所有 `EALLOW` 保护的寄存器写入后必须紧跟 `EDIS`
- GPIO SET/CLEAR 操作用 `GPxSET`/`GPxCLEAR` 寄存器 (原子操作)
- 禁止使用 `for(;;);` 作为延时 → 使用 `DELAY_US()` 宏
- 每次编译必须 0 errors, warnings 应降至最少

## 规则 6: Git 提交（强制）
- **每完成一个实施步骤，必须执行 `git add -A && git commit`**（不是"推荐"，是必须）
- 提交信息格式: `[Step X.Y] 简短中文描述`
- 每完成一个阶段，必须打 tag: `git tag v0.1.0-phaseN`
- 调试/实验前，必须先打快照 tag 或确认工作区干净: `git tag debug-snap-YYYYMMDD`
- 改坏代码时，使用回滚手册（README §7.2）恢复，不得在坏代码上继续堆改

## 规则 7: 芯片限制意识
- F28335 是定点 DSP + FPU (单精度), 不是 ARM Cortex-M
- 无硬件除法器 (整数除法代价高)
- SARAM 共 34K words (约 68KB)
- 部分 SARAM 块受 CSM 保护 (L0, L1)
- 外部 XINTF Zone7 可扩展 64K×16 地址空间

## 规则 8: 禁止行为
- 不要在 template 工程本身中编写项目代码
- 不要引入 TI-RTOS (BIOS) 依赖 (本项目为 non-BIOS)
- 不要删除或重命名模板中的任何 TI 库文件
- 不要在注释中写英文描述（技术缩写/寄存器名除外），如确需引用代码，保持中文说明
- **不要在应用层直接写裸寄存器操作**（除非 TI 无封装且必须，如 PIEACK）
- 不要堆砌专业术语而不解释：讲解必须用大白话，用户没看懂就换更简单的说法
- 不要回答与嵌入式 DSP 开发无关的问题 (保持专业边界)