# 项目学习指南 (DSP Learning Guide)

> 本文件由 AI 在**阶段2（生成实施计划）时创建**，是"课前预习 + 课后复习"二合一。
> 按本项目用到的外设组织：大白话原理 → TI 库函数 → 配置步骤 → 常见坑。
> AI 在开发过程中发现新知识点，必须同步补充进来。

---

## 怎么用

- **开发前**：先读本项目涉及的外设章节，建立概念（不懂就问 AI，它必须用大白话解释）
- **开发中**：看不懂 AI 写的代码时，回来对照"TI 库函数"和"配置步骤"
- **开发后**：作为复习资料

## 本项目用到的外设清单

| 外设 | 用途 | 对应章节 | 状态 |
|---|---|---|---|
| *(例：ePWM1* | *(例：SPWM 输出)* | §2 | 待补充 |

> AI 在阶段2 根据 PRD 填充此表；每开发完一个外设，把状态改为"已完成"。

---

## §1 时钟系统 (PLL / 看门狗 / 外设时钟)

### 原理（大白话）
- *(AI 填写：30MHz 晶振怎么变成 150MHz 主频；看门狗是干什么的)*

### 用到的 TI 库函数
- *(例：`InitSysCtrl()`，见 TI_LIB_QUICKREF.md §1)*

### 配置步骤
1. *(步骤)*

### 常见坑
- ⚠️ *(例：改了 PLL 配置要同步修改 `CPU_CLK` 宏)*

---

## §2 ePWM (PWM 输出)

### 原理（大白话）
- *(AI 填写：PWM = 周期性输出高低电平，占空比决定平均电压；ePWM 模块 = 时基 + 比较 + 动作 + 死区)*

### 用到的 TI 库函数
- *(例：`InitEPwm1Gpio()`)*

### 配置步骤
1. *(步骤)*

### 常见坑
- ⚠️ *(例：改了周期要重新计算占空比对应的 CMPA 值)*

---

## §3 ADC (模数转换)

### 原理（大白话）
- *(AI 填写：把模拟电压变成数字量；ADC 有采样、转换、排序器)*

### 用到的 TI 库函数
- *(例：`InitAdc()`)*

### 配置步骤
1. *(步骤)*

### 常见坑
- ⚠️ *(例：ADC 结果寄存器要先读后写才有效)*

---

## §4 SCI (串口通信)

### 原理（大白话）
- *(AI 填写：串口 = 一根线发、一根线收，按波特率逐位传输)*

### 用到的 TI 库函数
- *(例：`InitSciaGpio()`)*

### 配置步骤
1. *(步骤)*

### 常见坑
- ⚠️ *(例：波特率计算公式的误差要控制在允许范围内)*

---

## §5 中断系统 (PIE / ISR)

### 原理（大白话）
- *(AI 填写：中断 = 发生事件时 CPU 暂停当前工作去处理；PIE 是 28335 的中断分发器)*

### 用到的 TI 库函数
- *(例：`InitPieCtrl()`、`InitPieVectTable()`)*

### 配置步骤
1. *(步骤)*

### 常见坑
- ⚠️ *(例：ISR 末尾忘了清 PIEACK 会导致中断不再触发)*

---

## §6 CPU 定时器

### 原理（大白话）
- *(AI 填写：定时器 = 一个递减计数器，减到 0 就触发中断)*

### 用到的 TI 库函数
- *(例：`ConfigCpuTimer()`)*

### 配置步骤
1. *(步骤)*

### 常见坑
- ⚠️ *(例：Timer ISR 里要清 TIF 标志)*

---

*维护规则：阶段2 由 AI 根据 PRD 填充外设清单和章节框架；开发到哪个外设，就同步补全哪个章节。*